<tool id="infofile_check_id" name="Info file check" version="1.0.0">

    <description>Checks the content of the RAxML tree info file for pplacer</description>
    
	<command interpreter="python">
		./infofile_check_wrapper.py $input $output
	</command>
	
	<inputs>
		<param name="input"	type="data" format="txt" label="Info file to check"/>
	</inputs>
	
	<outputs>
		<data name="output" format="txt" label="Info file checked"/>
	</outputs>

	<help>
		Parses the RAxML tree info file (evolutionary model), and removes the line making pplacer bug (new RAxML version not supported).
	</help>

</tool>

